/*#pragma once

#include "VanK/Core/core.h"

namespace VanK
{
    class Texture
    {
    public:
        static SDL_Surface* LoadImage(const char* imageFilename, int desiredChannels);
        static void CreateTexture(const char* imageFilename, int desiredChannels);
        static void CreateTexture2D(const char* imageFilename, int desiredChannels);
        static SDL_GPUTexture* CreateTexture2D(SDL_Surface* imageData);
        static SDL_GPUTexture* CreateTexture(SDL_Surface* imageData);
    };
}*/